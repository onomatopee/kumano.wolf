let aliveList =  [];
let nameList = [];
let attrList = [];
let attrNum = {};

function generateRyosei() {
  const ryoseiNum = attrList.length();
  for (let i = 0; i < ryoseiNum; i++) {
    aliveList.push(true);
  }
}

function chooseClaimer() {
  ok = false
  while (!ok) {
    const candidate = Math.floor(Math.random() * (i + 1));
    
  }
}